// Not implemented yet — this is a Phase 4 module (DESIGN.md §14, §9.2).
//
// Scope when it lands: Lensfun database lookup by camera+lens+focal+aperture,
// parsing Sony's embedded ARW lens-correction data as a fallback source, and
// the lens_overrides table (DESIGN.md §5.4) for disambiguating third-party
// lenses reported under ambiguous Nikon lens IDs (the Tokina 100mm case).
