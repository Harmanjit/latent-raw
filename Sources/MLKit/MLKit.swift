// Not implemented yet — this is a Phase 5 module (DESIGN.md §14, §8.4).
//
// Scope when it lands: Vision-based subject/person segmentation, a Core ML
// sky-segmentation model running on the ANE, and IOSurface-backed mask
// caching so masks are shared with PixelEngine without copies.
